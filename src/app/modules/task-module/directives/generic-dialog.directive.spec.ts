import { GenericDialogDirective } from './generic-dialog.directive';

describe('GenericDialogDirective', () => {
  it('should create an instance', () => {
    const directive = new GenericDialogDirective();
    expect(directive).toBeTruthy();
  });
});
